{
  "name": "api",
  "version": "1.0.0",
  "description": "server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "json-server --port 3001 --watch db.json",
    "start:dev": "nodemon --watch db.json --exec npm run start:server"
  },
  "author": "ahmet burak karhan",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
