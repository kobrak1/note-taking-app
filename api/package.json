{
  "name": "api",
  "version": "1.0.0",
  "description": "server",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../client && npm run build && cp -r build ../api",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "build": "npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "start:server": "json-server --port 3001 --watch db.json",
    "api:dev": "nodemon --watch db.json --exec npm run start:server",
  },
  "author": "ahmet burak karhan",
  "license": "MIT",
  "devDependencies": {
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.3"
  }
}
