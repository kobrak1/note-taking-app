const HeaderItem = ({children}) => {
    return (
        <li className="text-slate-500 list-none m-1 px-2 py-1 cursor-pointer">
            {children}
        </li>
    )
}

export default HeaderItem